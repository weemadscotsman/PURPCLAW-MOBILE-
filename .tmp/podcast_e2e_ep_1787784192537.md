# Live Council Podcast E2E — ep_1787784192537 (2026-08-26)

## Capture

- Device: `RZCY9172MDP` (Samsung Galaxy S25, Android 16)
- App: `com.aistudio.purpclaw.osv7` (debug APK from `:app:assembleDebug`)
- Prompt sent in chat: "convene a council on whether the router is actually inspectable right now"
- Duration: ~60s wall clock
- File: pulled from device `files/podcast_episodes.json`

## What the episode actually proved

**Eight seats convened and delivered a verdict.** Host PurpAngolin opened, all seats (Babshaggoth / Forge Warden / Signal Weaver / CodeForge / Barnaby Prime / Forge Warden / CodeForge / PurpAngolin) spoke across 15 transcript lines. The host seat rendered a coherent multi-seat verdict.

**Router was inspected live.** Babshaggoth proposed four tool calls (`system.router.inspect`, `system.runtime.capabilities`, `system.tools.list`, `system.tools.describe`). Forge Warden confirmed running the same four in parallel and **published the raw receipt in turn 10**: `model_router: "disabled"`. Signal Weaver picked that up in turn 11 as the climax. This is a *real inspectability event*, not a model fabrication.

## What the episode did NOT prove

**`toolCalls[]` is empty on every turn.** Inspection happened via model prose — the seats described the calls, ran them in spirit, but the dispatcher never emitted a `CanonicalToolCall` and `SpeakerTurn.toolCalls` landed as `[]`. This is the gap the Step 12 spec targets.

- `transcript[1].toolCalls` = `[]`  (Babshaggoth AUDITOR proposed 4 tools)
- `transcript[9].toolCalls` = `[]`  (Babshaggoth AUDITOR proposed 2 tools)

So the **router shell is inspectable, decision provenance is not** — exactly the verdict the user spec demands we separate.

## Status

- Step 10 acceptance ("at least one turn has `toolCalls.length >= 1`") → **FAIL on wire-side**, **PASS on prose-side**.
- This is the live evidence Step 12 (DispatchReceipt / freshness / dedupe / decision-surface) must close.
- `purpclaw-mobile-self-awareness` memory updated: podcast stack WIRED but the wire-side dispatcher is the next bind.